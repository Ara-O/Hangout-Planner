export type UserLocationInformationType = {
  username: string;
  longitude: number | null;
  latitude: number | null;
};

export type LocationType = {
  longitude: number | null;
  latitude: number | null;
};
